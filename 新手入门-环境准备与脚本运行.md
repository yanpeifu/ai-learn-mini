# 新手入门：环境准备与脚本运行

> 面向零基础。读完这一篇，你就能独立跑通出题质量检测。
> 预计耗时：20 分钟（其中 15 分钟是注册账号）。

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 撰写日期 | 2026-09-14 |
| 配套脚本 | `E:\CodexProjects\ai-learn-mini\quality_check.py` |

---
基础环境准备：python 3.14.5 、pip 26.1.1、脚本依赖：按需安装 、DeepSeek接口、API Key

## 0. 先看结论

我检查过你的电脑，**环境基本已经就绪**，你实际只需要做三件事：

| 步骤 | 要做的事 | 耗时 | 花钱吗 |
| --- | --- | --- | --- |
| 1 | 申请一个 DeepSeek API Key | 15 分钟 | 充值 10 元起 |
| 2 | 把 Key 配置到环境变量 | 1 分钟 | 不花 |
| 3 | 跑脚本看报告 | 3 分钟 | 约 0.3 元 |

Python 已经装好了，不用再折腾。

---

## 1. 这个脚本到底是干什么的

### 1.1 一句话解释

**它是你「自动出题工厂」里的质检员。**

想象你开了一家工厂：顾客扔进来一段知识文字，机器吐出一套带选项和答案的题目。
这个脚本做的事，就是把这些题目拿去**逐项检查**，最后交给你一份质检报告。

### 1.2 它实际执行的四步

```
第一步 准备原料
       脚本内置了 20 段不同领域的知识文字
       （货币政策、光的折射、细胞呼吸、行测资料分析、认知失调……）
              ↓
第二步 调用 AI 出题
       把每段文字发给大模型，要求它按固定格式输出：
       知识点大纲 + 8~12 道题 + 每题的讲解
              ↓
第三步 逐项检验
       对返回的题目做 12 类自动检查
              ↓
第四步 打分出报告
       每份结果算 0~100 分，汇总成一份 Markdown 报告
```

### 1.3 它具体检查什么

我用大白话解释几个最有代表性的检查项：

| 检查项 | 大白话解释 | 为什么可怕 |
| --- | --- | --- |
| 答案不在选项里 | 正确答案写的是 E，但选项只有 A/B/C/D | 用户永远答不对，这是致命缺陷 |
| 选项重复 | 两个选项内容一模一样 | 题目实际上没有唯一解 |
| 无效选项 | 出现「以上都对」这种选项 | 教研上公认的无效干扰项，考察不出理解 |
| 判断题选项数错误 | 判断题给了 4 个选项 | 格式混乱，说明模型没听懂要求 |
| 多选题只给一个答案 | 要求选多个，答案字段只有 1 个 | 用户按多选题做，必然判错 |
| 讲解与答案矛盾 | 讲解里**断言**答案是 C，但答案字段是 A | 模型自己都没想清楚，用户会彻底懵 |
| 题干重复 | 题干相似**且选项也重合**，基本是同一道题 | 用户觉得产品粗糙，还白花了生成成本 |
| 答案全集中在一个选项 | 10 道单选题答案是 A 的有 8 道 | 用户会靠猜规律作答，而不是靠理解 |
| 难度完全一样 | 所有题都是中等难度 | 没有难易起伏，就构不成「闯关」体验 |
| 知识点没覆盖到 | 大纲里有 4 个知识点，有 1 个没出题 | 闯关进度和学习目标对不上 |

完整的 12 类检查项在 `README.md` 里有表格。

> **注意**：有些情况看起来像缺陷，其实是好的出题设计——例如
> 「求基期量公式是？」与「求增长量公式是？」这种句式相同的并列出题，
> 以及讲解里解释错误选项（「故不选D」）。检查器刻意不报这些，
> 具体原因见 `README.md` 的「刻意不做的检查」一节。

### 1.4 评分规则

满分 100 分，每个错误扣 8 分，每个警告扣 3 分。

**怎么解读分数**：

| 分数 | 含义 | 该怎么办 |
| --- | --- | --- |
| 85–100 | 质量良好 | 进入人工抽检，确认事实准确性 |
| 70–85 | 基本可用，有瑕疵 | 针对高频问题修提示词 |
| 50–70 | 问题较多 | 需要认真重写提示词 |
| 低于 50 | 不可用 | 换模型或重新设计提示词策略 |

---

## 2. 它对你的项目意味着什么

这件事值得花时间，是因为它影响的不是"某个功能"，而是**整个产品的地基**。

### 2.1 决策层面（最重要）

这个产品成立的地基假设是：**「AI 自动生成的题目质量足够好」**。

如果这个假设不成立——答案经常错、选项模棱两可、讲解是废话——那么你后面做的所有东西，
漂亮的答题动画、精致的分享海报、完整的会员体系，**全部都没有意义**。

而这个假设在开发阶段是看不出来的。你必须真的生成一大批题、逐条检查，
才知道模型行不行、提示词写得好不好。

这个脚本用**半天时间 + 几毛钱**，给你一个可量化的答案。
对比一下：等你把前端后端都写完，才发现题目质量不行，那是两个月的返工。

### 2.2 开发层面

两件事可以直接复用：

1. **提示词可以直接搬**。脚本里那份提示词经过验证后，就是后端代码里要用的那一份。
   你不需要在写后端的时候再重新试错一遍。
2. **检查项就是验收标准**。脚本里的 12 类检查，本质上定义了"什么样的题目算合格"。
   后端的出题接口也应该满足这些标准——甚至可以直接把部分检查逻辑搬到服务端，
   做到"不合格的题不让它出现在用户面前"。

### 2.3 作品集层面

这一条容易被忽略，但对你来说可能最实际。

面试时的两种说法，感受完全不同：

> ❌「我做了个 AI 出题的小程序。」

> ✅「我做了一个 AI 出题产品。因为核心风险是生成质量不可控，我先设计了一套包含 12 项指标的
> 质量评估体系，用 20 个跨领域样例跑基线，发现多选题答案数量和讲解一致性是最容易出问题的地方，
> 迭代了 4 版提示词把平均分从 62 提到 88，然后才开始写业务代码。」

第二种说法证明的是一种更稀缺的能力：**你知道怎么衡量 AI 产品的质量**。
现在市面上会调 API 的人很多，会做质量评估的人很少。

---

## 3. 它做不到什么（必须知道）

自动检查只能发现**结构性缺陷**。以下三类它查不出来，必须人工判断：

1. **事实是否准确**。比如把「光速约 30 万公里/秒」写成 3 万，脚本查不出来。
2. **题目是否有歧义**。两个选项都说得通，脚本也查不出来。
3. **是否符合你给的原文**。模型可能引入外部知识，甚至编造内容。

所以正确的流程是两步：

```
脚本自动筛掉低级错误  →  人工抽查事实准确性
```

**人工抽检怎么做**：从每份结果里随机挑 3 道题，对照原文核对答案和讲解。
20 个样例一共抽 60 道题，大约需要一小时，这一小时不能省。

---

## 4. 环境准备：你的电脑现状

我先替你检查了一遍，结论是**大部分准备工作已经完成了**：

| 检查项 | 状态 | 说明 |
| --- | --- | --- |
| Python | ✅ 已安装 | 版本 3.14.5，位置 `D:\Program Files\Python\python.exe` |
| pip（包管理器） | ✅ 已安装 | 版本 26.1.1 |
| 脚本依赖 | ✅ 不需要安装 | 脚本只用 Python 标准库，不用 `pip install` 任何东西 |
| 项目文件 | ✅ 已就位 | `E:\CodexProjects\ai-learn-mini` |
| DeepSeek 接口连通性 | ✅ 可达 | 访问返回 401，这是正常的（表示需要密钥） |
| API Key | ❌ **还没有** | 这是你唯一需要补的东西 |

所以真正的准备工作只有一件：**申请 API Key**。

---

## 5. 第一步：申请 DeepSeek API Key

### 5.1 操作步骤

1. 浏览器打开 <https://platform.deepseek.com>
2. 注册并登录（手机号或邮箱都可以）
3. 完成**实名认证**（国内 API 服务都需要，按提示上传身份证信息）
4. **充值**：最低 10 元。跑完 20 个样例大约只花 3 毛钱，10 元足够你反复测试很久
5. 在左侧菜单找到「**API Keys**」，点击「创建 API Key」
6. **关键：Key 只会完整显示一次**，创建后立刻复制，粘贴到安全的地方保存

Key 的格式长这样：

```
sk-1a2b3c4d5e6f7g8h9i0j1k2l3m4n5o6p
```

### 5.2 如果不想用 DeepSeek

任何**兼容 OpenAI 接口格式**的服务都可以用，只需要改两个环境变量。
例如换成阿里云百炼（通义千问）：

```powershell
$env:LLM_BASE_URL = "https://dashscope.aliyuncs.com/compatible-mode/v1"
$env:LLM_MODEL    = "qwen-plus"
$env:LLM_API_KEY  = "sk-你的阿里云key"
```

**为什么建议先用 DeepSeek**：价格最低，而且它的输出质量和结构化能力对出题场景够用。
等你验证完质量，如果决定产品化，再考虑切换到能提供《合作协议》的厂商
（原因见《技术选型分析》第 5.2 节）。

---

## 6. 第二步：配置 API Key（用 .env 文件）

### 6.1 .env 文件已经帮你创建好了

位置：

```
E:\CodexProjects\ai-learn-mini\.env
```

**你只需要做一件事**：用记事本打开它，把这一行

```
DEEPSEEK_API_KEY=sk-替换成你自己的key
```

改成

```
DEEPSEEK_API_KEY=sk-你的真实key
```

然后保存。注意事项：

- 等号两边**不要加空格**
- Key **不要加引号**
- 把「替换成你自己的key」这几个中文字**完整删掉**，换成你的真实 Key

脚本会自动读取这个文件，不需要再做其他配置。

### 6.2 怎么打开和编辑

**方式一（最简单）**：文件资源管理器里进入 `E:\CodexProjects\ai-learn-mini`，
右键 `.env` → 打开方式 → 记事本。改完按 `Ctrl + S` 保存。

**方式二（PowerShell 一句话打开）**：

```powershell
notepad E:\CodexProjects\ai-learn-mini\.env
```

> 提示：`Ctrl + H` 打开记事本的替换功能，可以快速把占位文字替换成真实 Key。

### 6.3 验证配置是否正确

```powershell
cd E:\CodexProjects\ai-learn-mini
python quality_check.py --limit 1
```

配置正确的话，会先打印出这样两行：

```
[配置] 已加载 .env：E:\CodexProjects\ai-learn-mini\.env
[配置] API Key：sk-1a2******5o6p
```

Key 是**脱敏显示**的（只显示开头 6 位和结尾 4 位），不会被完整打印出来。

如果看到的是这个错误，说明你还没替换占位符：

```
[错误] 读取到的 API Key 看起来还是示例占位符：sk-替换成******的key
```

### 6.4 备选方式：设置系统环境变量

如果你不想用 `.env`，也可以用系统环境变量。
**环境变量的优先级高于 `.env`**，两者同时存在时以环境变量为准。

**只对当前窗口有效：**

```powershell
$env:DEEPSEEK_API_KEY = "sk-你的key"
```

**永久保存到用户账户：**

```powershell
[Environment]::SetEnvironmentVariable("DEEPSEEK_API_KEY", "sk-你的key", "User")
```

设置完之后**必须关掉窗口重新打开**才会生效，因为环境变量是在窗口启动时读取的。

### 6.5 一个必须遵守的原则

`.env` 文件已经被 `.gitignore` 排除，不会被提交到 Git。

但**千万不要把 Key 直接写进 `.py` 代码文件**。写进代码里之后，哪怕只提交一次，
Key 就永久留在了 Git 历史记录里，删都删不干净。

---

## 7. 第三步：运行脚本

### 7.1 打开 PowerShell 并进入目录

按 `Win + R`，输入 `powershell`，回车。然后执行：

```powershell
cd E:\CodexProjects\ai-learn-mini
```

### 7.2 ① 先跑离线自检（不需要 Key，不花钱）

```powershell
python quality_check.py --mock
```

**预期输出**：

```
[quality_check 0.1.0] 模式=离线模拟 样例=2 模型=内置模拟数据
----------------------------------------------------------------------
[1/2] s01 金融·货币政策三大工具 ... 得分 11（错误 10 / 警告 3） 0.0s
[2/2] s02 物理·光的折射与全反射 ... 得分 100（错误 0 / 警告 0） 0.0s
----------------------------------------------------------------------
平均得分 55.5/100  |  错误 10  |  警告 3
报告：E:\CodexProjects\ai-learn-mini\reports\quality_report_xxxx.md
```

**看到这个说明脚本工作正常。** 解释一下这两条：

- `s01` 得分很低，是**故意做坏的**数据，用来证明检查器真的能发现问题
- `s02` 得 100 分，是**标准答案**，用来证明检查器不会冤枉好数据

如果这里报错了，去看第 9 节的排查表。

### 7.3 ② 跑 3 个样例试水（花不到 1 毛钱）

确认离线自检通过、API Key 也配好之后：

```powershell
python quality_check.py --limit 3
```

这一步的目的是**确认密钥配置正确、网络通畅、成本可控**，不要一上来就跑 20 个。

### 7.4 ③ 跑完整的 20 个样例（约 3 毛钱）

```powershell
python quality_check.py
```

### 7.5 ④ 用你自己的文字测试（这才是最终要验证的）

内置样例只能验证"通用能力"，真正要验证的是**你的目标用户会输入的内容**。

```powershell
python quality_check.py --text "光合作用是植物利用光能，把二氧化碳和水转化成有机物并释放氧气的过程。光合作用分为光反应和暗反应两个阶段……"
```

或者把内容存成 txt 文件：

```powershell
python quality_check.py --file "D:\我的资料\行测笔记.txt"
```

**建议**：从你熟悉的领域找 3~5 段真实材料（比如考公的行测讲义片段），
这比内置样例更能说明问题。

### 7.6 ⑤ 只看提示词，不调用 API（不花钱）

```powershell
python quality_check.py --dry-run
```

它会打印出即将发送给大模型的完整提示词。想自己改提示词时，先用这个确认改动内容。

---

## 8. 第四步：看懂报告

报告生成在 `E:\CodexProjects\ai-learn-mini\reports\` 目录下，每次运行都会生成两个文件：

| 文件名 | 用途 |
| --- | --- |
| `quality_report_<时间戳>.md` | 人看的报告：得分、问题清单、改进建议 |
| `raw_<时间戳>.json` | 机器存档：模型的原始输出，便于逐题人工核对 |

### 8.1 报告里重点看三节

**第 1 节「总览」** —— 看平均得分。
对照第 1.4 节的分数表，判断当前质量处在什么水平。

**第 3 节「问题类型分布」** —— 看哪类问题最多。
一张按出现次数排序的表，**排在第一行的问题类型，就是你下一版提示词要重点修的地方**。

**第 4 节「问题明细」** —— 看每一条问题的具体说明。
格式是「题号 + 问题代码 + 说明」，比如：

```
- 🔴 错误 `q5` `option_low_quality` — 使用了低质量选项（如「以上都对」），教研上视为无效干扰项
```

### 8.2 拿到报告后该做什么

```
① 先看错误（红色），再看警告（黄色）
        ↓
② 把出现次数最多的问题，写成提示词里的硬性约束
   例如反复出现「以上都对」，就在提示词里点名禁止
        ↓
③ 重新跑一遍，对比平均分是否提高
        ↓
④ 重复 ②③，直到自动检查基本无错误
        ↓
⑤ 人工抽检：每份结果挑 3 道题，对照原文核对事实
```

**核心方法**：每次改提示词都用分数衡量效果，**不要凭感觉判断提示词变好还是变坏**。

---

## 9. 常见报错排查

| 报错信息 | 原因 | 解决办法 |
| --- | --- | --- |
| `python 不是内部或外部命令` | Python 没加入 PATH | 你的电脑已配好，正常不会遇到 |
| `读取到的 API Key 看起来还是示例占位符` | `.env` 里还是那行中文占位文字 | 打开 `.env`，把 `sk-替换成你自己的key` 换成真实 Key |
| `[错误] 未找到 API Key` | `.env` 不存在且环境变量也没设 | 确认 `.env` 文件在 `ai-learn-mini` 目录下，文件名不要变成 `.env.txt` |
| `[配置] 未找到 .env 文件` | 文件位置不对或文件名不对 | 检查是不是被存成了 `.env.txt`（资源管理器要打开「显示文件扩展名」） |
| `HTTP 401` | Key 错误、过期，或复制时多了空格。**脚本会自动中止本次运行** | 去控制台删掉重新创建，粘贴后检查首尾有没有空格，再重跑 |
| `HTTP 402` 或 `Insufficient Balance` | 账户余额不足。**脚本会自动中止**，不会把剩余样例白跑一遍 | 充值后重新运行；已完成的结果仍会生成报告 |
| 屏幕出现 `[已中止]` 提示 | 这是保护机制，遇到密钥/余额问题就停下 | 按提示处理问题后重跑即可 |
| `HTTP 429` | 请求太频繁被限流 | 等 1 分钟再跑 |
| `调用大模型失败：timed out` | 网络慢或模型响应慢 | 在 `.env` 里把 `LLM_TIMEOUT` 改成 `300` 后重跑 |
| `WinError 10013` / 连接被拒绝 | 本机防火墙或代理拦截 | 检查代理软件，或临时关闭防火墙后重试 |
| 中文显示成乱码 | 终端编码问题 | 脚本已自动处理；仍乱码可先执行 `chcp 65001` |
| `无法从返回内容中解析出 JSON` | 模型没按要求输出 JSON | 偶尔出现属正常，重跑即可；频繁出现需加强提示词约束 |

---

## 10. 成本预估

| 操作 | 花费 |
| --- | --- |
| 离线自检 `--mock` | 0 元 |
| `--dry-run` | 0 元 |
| 跑 3 个样例 | 约 0.05 元 |
| 跑 20 个样例 | 约 0.3 元 |
| 完整流程（含反复迭代提示词 10 次） | 通常不超过 3 元 |

**计算依据**：DeepSeek flash 空闲时段定价为输入 1 元/百万 token、输出 4 元/百万 token。
单次出题大约消耗 600 输入 token 和 3500 输出 token，折合每次约 0.015 元。

也就是说，**验证这个核心假设的总成本不到一杯奶茶的钱**。

---

## 11. 安全提醒

**API Key 等同于你的钱包。** 泄露后别人可以拿它消耗你的余额，甚至用它做违规的事。

必须遵守的几条：

1. **不要把 Key 直接写进代码文件**（尤其不要写死在 `.py` 里）
2. **不要把 Key 提交到 GitHub**。项目里的 `.gitignore` 已经排除了 `.env` 文件
3. **不要在截图、聊天记录、录屏里暴露完整的 Key**
4. **万一泄露了**：立刻去 DeepSeek 控制台删除那个 Key，然后新建一个
5. **给项目单独建一个 Key**，不要和其他项目混用，方便随时吊销

---

## 12. 一页速查

```powershell
# 进入项目目录
cd E:\CodexProjects\ai-learn-mini

# 编辑配置（把 .env 里的占位符换成真实 Key）
notepad .env

# 离线自检（不需要 Key，不花钱）
python quality_check.py --mock

# 试水（验证 Key 配置是否正确）
python quality_check.py --limit 1

# 备选：环境变量方式（优先级高于 .env）
[Environment]::SetEnvironmentVariable("DEEPSEEK_API_KEY", "sk-你的key", "User")

# 完整检测
python quality_check.py

# 用自定义文字
python quality_check.py --text "你的知识内容"
python quality_check.py --file "路径\文件.txt"

# 只看提示词
python quality_check.py --dry-run

# 查看报告目录
explorer E:\CodexProjects\ai-learn-mini\reports
```

---

## 13. 完成之后

跑完并拿到基线分数之后，把这两个数字告诉我：

1. **20 个样例的平均得分**
2. **出现次数最多的前三类问题**

我会根据结果帮你做下一步：优化提示词、调整评分权重，或者判断是否需要换模型。
质量验证通过后，就可以进入项目骨架搭建了。

---

*如果你在任何一步卡住了，把终端里的完整报错信息复制给我，我来判断问题在哪。*
