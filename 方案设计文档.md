# AI 闯关学习小程序 · 方案设计文档（开发前确认稿）

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.1（决策已确认，可开工） |
| 撰写日期 | 2026-09-17 |
| 对应需求 | 《产品需求分析文档》v1.1、《技术选型分析》v1.0、《UI设计风格方案》v1.2 |
| 对应原型 | `产品原型/原型-0-入口导航.html`、`原型-1-核心流程.html`（18 屏）、`原型-2-扩展功能.html`（7 屏）、`原型-3-状态与规范.html` |
| 开发目录 | `E:\CodexProjects\ai-learn-mini` |
| 本次范围 | MVP（V0.1）核心闭环：输入 → 大纲 → 出题 → 答题 → 讲解 → 结算 + 学习记录 |

> **本文档的用法**：先看第 1 章（我对你资源与任务的理解，确认我没有理解错），
> 再看第 2 章（**需要你拍板的 10 件事**，这是唯一的阻塞项），
> 第 3 章之后是我准备执行的具体方案与步骤。确认后我就按第 8 章的顺序开工。

---

## 1. 我对资源和任务的理解（请核对）

### 1.1 产品是什么

把任意一段知识文本，变成一条游戏化学习路径：用户输入 20–2000 字 → AI 拆出 3–5 个知识点
（可编辑）→ 生成 3 关 × 5 题 = 15 题 → 逐题作答、提交后立即判定并展示讲解 → 三关完成出结算
（正确率 / 用时 / 星级 / 薄弱知识点 / 指向性建议）→ 学习记录可回看。

MVP 要验证的假设是 **H2「闯关形式让人更愿意坚持」**（H1 出题质量已验证 20/20、99.2 分）。
所以 **出题质量保障（L8）与答对答错的即时反馈（L11 基础版）必须留在 MVP**，
而关卡地图 / 自适应难度 / 苏格拉底引导 / 间隔重复 / 分享海报 / 复盘报告全部属于 V0.5。

### 1.2 技术栈（已确认的决策，我不改动）

| 层 | 选型 |
| --- | --- |
| 小程序前端 | Taro 4 + React 18 + TypeScript（strict）+ NutUI（仅基础控件按需引入） |
| 后端 | Python 3.11+ / FastAPI + SQLAlchemy 2.x + Alembic + Pydantic v2 |
| 数据库 | 开发期 SQLite → 生产 PostgreSQL |
| 大模型 | **LangChain（OSS Python）+ DeepSeek（开发期）→ 百炼 / 火山（产品化）**；框架只允许出现在 LLM 适配层，业务代码依赖自研 `LLMProvider` 门面接口 |
| 状态管理 | React Context + Hooks（不上 Zustand） |
| 后端测试 | pytest（TDD） |
| 部署 | 本次不做（本地开发，服务器/备案暂缓） |

**两条边界（2026-09-17 已确认）**：

- **使用 LangChain**：技术选型第 10 节原本写的是「不建议引入 LangChain / LlamaIndex」，
  按你的明确要求，本方案改为**必须使用 LangChain**，同时加三条约束，避免框架渗透到业务代码：
  ① 框架只允许出现在 `backend/app/services/llm/` 这一层；
  ② 业务代码只依赖自研的 `LLMProvider` 门面接口（`chat` / `chat_json`），不 import 任何 `langchain*`；
  ③ 单元测试用 LangChain 官方 `GenericFakeChatModel` + 自研 `MockProvider` 双轨，零网络、零成本、可重复。
- **不在小程序端直连大模型**（Key 会泄露，且小程序要求域名备案）。

### 1.3 UI 还原的基准（我会严格按这个执行）

设计令牌（来自《UI设计风格方案》v1.2 与 `原型-3-状态与规范.html` 第五节，二者一致）：

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| 纸张底 / 卡片 | `#FFFCF7` / `#FFFFFF` | 页面与卡片 |
| 墨线 / 主文字 | `#33302E` | 所有描边、主文字 |
| 暖阳橙 / 按压 | `#FFA85C` / `#EE9247` | 主按钮、进度条 |
| 薄荷绿 / 深绿 | `#7ED9A8` / `#2A7D57` | 答对、掌握 |
| 珊瑚红 / 深红 | `#FF9A9A` / `#C4564F` | 答错填充 / 错误文字 |
| 薰衣草紫 / 暖黄 | `#B9A6F5` / `#FFE08A` | 星级 / 提示条 |
| 次级文字 / 分隔线 | `#8A8078` / `#F0E9E0` | 说明文字 / 分隔 |
| 描边 `--bw` | `2.5px solid #33302E` | 所有描边 |
| 圆角 | 卡片 24 / 按钮·输入框 18 / 选项条 16 / 小标签 12 | — |
| 硬投影 `--hard` | `0 2px 0 #33302E` | 漫画感（v1.2 已从 3px 收到 2px） |
| 卡片阴影 `--soft` | `--hard` + `0 10px 20px rgba(51,48,46,.06)` | 二级卡片、题干、弹窗 |
| 内高光 `--gloss` | `inset 0 2px 0 rgba(255,255,255,.65)` | 黏土手感 |
| 按压 | `translateY(2px)` + 投影收为 0，`120ms cubic-bezier(.34,1.56,.64,1)` | 全部可点元素 |
| 字号 | 展示 40/800 · H1 22/800 · H2 17/700 · 正文 14.5/1.7 · 最小 12 | 中文一律系统字体（主包 2MB 限制） |
| 配色铁律 | **橙底/绿底/黄底配墨黑字；白字只能配 `#C43D3D` 深红** | 对比度 ≥4.5:1 |

**吉祥物「颜宝」**：原型里的 8 个表情是内联 SVG `symbol`（`yb-happy / yb-think / yb-star /
yb-shy / yb-cheer / yb-peek / yb-sad / yb-go`）。我会把这段 SVG **原样移植**成一个
`<Yanbao mood="..." />` 组件（不重画、不引入图片、不改变造型），保证与原型完全一致。

**文案**：一律以原型里的原文为准（原型若与 PRD 措辞不同，以原型为准，见第 2 章第 1 条）。

### 1.4 原型的 18 屏 → 页面对应表（我要实现的清单）

| 原型屏 | 页面 | 我实现的关键点 |
| --- | --- | --- |
| P1-1 首页·空态 | `pages/index/index` | 标题「今天想学点什么？」+ 副标题 + 开心颜宝；输入卡占位文案与右下角 `0 / 2000`；「试试这些」3 张可见模板卡（横向滑动共 6 个）；主按钮置灰；虚线引导空态卡 |
| P1-2 首页·学习中 | 同上 | 「继续上次的学习」暖黄卡（主题 · 第 N 关 n/5）+「最近学习」3 条记录（91% 绿 / 78% 灰 / 62% 红） |
| P1-3 首页·校验错误 | 同上 | 输入卡红描边 + 计数变红 + Toast「内容太短啦，再多描述一点（至少 20 字）」+ 灯泡提示条 + 按钮置灰 |
| P2-1 大纲·生成中 | `pages/outline/outline` | 思考颜宝 +「颜宝正在读你的内容 / 大约需要 20 秒」+ 三步指示（①理解内容 ②梳理结构 ③准备出题，循环高亮）+ 4 条 shimmer 骨架屏 + 底部提示条 |
| P2-2 大纲·可编辑 | 同上 | 提示条「梳理出 N 个知识点…」；知识点卡（序号圆标 + 标题 + 一句话说明 + ✕）；点标题行内编辑（橙色描边 +「编辑中… 点击标题即可改」+ ✓）；**左滑露出红色删除**；剩余不足 2 个禁删并提示；底部「重新生成」+「开始闯关」 |
| P2-3 大纲·生成失败 | 同上 | 委屈颜宝 +「这次没梳理成功」+ 安抚文案 + 暖黄「可以试试」建议条 +「改内容」/「再试一次」；**绝不显示错误码** |
| P3-1 单选未提交 | `pages/quiz/quiz` | 顶部「← 第 2 关 · 1 / 5 ⋯」+ 进度条 + 题型/难度标签 + 题干卡 + A/B/C/D 选项（选中暖黄 + 下沉 2px）+ 提示条「已选 A，提交前还可以改」+ 提交按钮 |
| P3-2 答对 | 同上 | 薄荷绿选项 + 「答对了」角标 + 其余选项虚线降透明 + 讲解卡（✓ 就是它）+ 底部「内容由 AI 生成 · 有问题可点右上角 ⋯ 反馈」+「下一题」 |
| P3-3 答错 | 同上 | 珊瑚红 + 左右轻晃（≤300ms）+ 「你选的」与「正确答案」同时可见 + 讲解卡「这题有点绕，我们来看看」 |
| P3-4 多选 | 同上 | 多选可勾选/取消、角标「已选」、提示条「多选题：全对才算对，目前选了 N 项」 |
| P3-5 判断 | 同上 | 两个大块 ✔/✘ 选项、未选时提交置灰 + 提示条 |
| P3-6 连对 3 题 | 同上 | 速度线暖黄横幅 + 放大数字「3 连对」+「手感不错！再答对 2 题就能拿下这一关」+ 欢呼颜宝 |
| P3-7 退出确认 | 同上 | 遮罩 + 弹窗（思考颜宝 +「先歇会儿？」+ 当前进度卡 +「继续答题」+ 下划线「退出，稍后再来」） |
| P3-8 举报 | 同上 | 右上角 ⋯ 触发；「这道题有什么问题？」4 个单选项（答案有误 / 表述不清楚 / 重复 / 其他）+「取消」/「提交反馈」+「同一题被 3 人反馈后会先下架复核」 |
| P4-1 三星结算 | `pages/result/result` | 欢呼颜宝 + 3 颗紫色星星逐个弹入 + 大号正确率 + KPI（用时 / 答对）+ 各知识点表现条（<60% 标红）+「再来一次」/「看学习记录」 |
| P4-2 低分鼓励 | 同上 | 鼓劲颜宝（`yb-go`）+ 亮/暗星 + 温和文案（不挫败）+ KPI（待复习错题 / 薄弱知识点） |
| P5-1 我的·有记录 | `pages/mine/mine` | 头像卡（颜宝 + 昵称「学习者」+ 非会员角标）+ 三格概览 + 记录列表（倒序、上拉加载）+ 底部「用户协议 · 隐私政策 · 版本 v0.1.0」 |
| P5-2 我的·空态 | 同上 | 探头颜宝 +「这里还空着」+「去学习」按钮 |
| 状态与规范 | 全局组件 | 按钮 5 态、选项条 7 态、标签与进度、卡片两级、空状态/网络错误/生成失败/生成中 4 个异常页、12 个线性图标、色彩与文字令牌 |

---

## 2. 需要你拍板的 10 件事（阻塞项）

每条都给出我的建议（默认值）。**你可以只回「1A 2A 3A…」这样的简写，或只指出不同意的几条，其余按我的建议执行。**

### Q1 PRD 里 F1 的「TODU 需实现包括文档」是什么意思？【最重要】

PRD 第 2.1 节功能表写成「F1 知识输入（**TODU 需实现包括文档**）」，但同一份 PRD 的其他条款互相矛盾：

| 位置 | 说法 |
| --- | --- |
| F1 详细说明 | 输入 = 纯文本框，**20–2000 字**，支持中英文数字标点 |
| 1.6 明确不做 | 视频 / 音频 / **网页解析**（理由：工程量最大、对验证核心假设帮助最小） |
| 3.11 L10 | MVP = 一句话/一段文字；**V0.5 = 网页链接；V1.0 = 文档（PDF/Word）**，理由是「解析 → 分块 → RAG，付费意愿最高但工程量中」 |
| 4.2 E7 | 文档上传解析（PDF / Word / txt）放 **V1.0**，前置条件：对象存储 |
| UI 原型 | 首页只有文本框 + 6 个模板卡，**没有任何上传/附件入口** |

- **A（我的建议）**：按 PRD 正文与原型执行 —— MVP 只做文本框输入，文档解析放 V1.0。
- **B**：MVP 就要支持文档上传（PDF / Word / txt）→ 我需要新增：`POST /api/knowledge/upload` +
  解析服务（pdfplumber / python-docx）+ 分块 + 前端上传入口。**但原型里没有这个界面**，
  按你「不要自行发挥」的要求，我需要你先给上传入口的设计（或授权我按首页同风格补一版给你确认）。

### Q2 扩展功能的 7 屏（V0.5）这次做不做？

`原型-2-扩展功能.html` 有 7 屏：EX-1 关卡地图、EX-2 错题本 + 间隔重复、EX-3 今日复习模式、
EX-4 可解释复盘报告（雷达图）、EX-5/EX-6 分享海报（成果型/挑战型）、EX-7 扫码挑战落地页。
PRD 与 UI 方案都把这一组标注为 **V0.5，并明确写了「不要在 MVP 阶段提前做」**。

- **A（我的建议）**：本次只做 MVP 的 P1–P5（18 屏状态全覆盖），扩展 7 屏留到 V0.5。
- **B**：一并实现（前端 + 后端预计增加约 40% 工作量：小程序码生成、Canvas 海报、
  ECharts 雷达图、间隔重复调度接口、错题本接口）。

### Q3 UI 与 PRD 的 4 处冲突，按哪个执行？

我已按「原型优先」定了默认，如果你不同意其中任何一条，请指出编号：

| 编号 | 冲突 | 我的默认 |
| --- | --- | --- |
| Q3-1 | **星级规则**：PRD 写「<50% 零星」，但原型 P4-2 的 47% 显示了 **1 颗亮星** | 逻辑按 PRD（<50% = 0 星），视觉沿用原型；若你要 47% 也显示 1 星，我改判定规则 |
| Q3-2 | P4-2 的按钮「先看看错题」指向 **V0.5 错题本** | MVP 保留按钮但点击提示「错题本马上就来，先再学一遍？」（不跳转死链）；或在 Q2 选 B 时正常跳转 |
| Q3-3 | P5-1 数据概览是 **3 格**（学习次数/累计答题/平均正确率），PRD F6 只写 2 项；且原型记录项**没有星级**，PRD 要求「主题、时间、正确率、星级」 | 概览按原型 3 格；列表项按原型（标题 + 正确率），并用颜色区分（绿 ≥90 / 灰 70–89 / 红 <70）。若你要星级，我在记录项右侧补小星（原型同风格） |
| Q3-4 | 首页模板：PRD 固定 **6 个**（含「自定义」空模板），原型只画了 3 个卡片（横向滑动） | 按 PRD 做 6 个横向滑动卡，后 3 个（英语语法 / 编程概念 / 自定义）沿用原型卡片的样式与线性图标 |

### Q4 输出「难度」标签只有 3 档，但数据模型是 1–5，怎么映射？

原型标签写作「难度 ★★☆ / ★★★ / ★☆☆」，而 PRD 要求 `difficulty` 取 1–5 且整套有梯度。

- **A（我的建议）**：数据库仍存 1–5（LLM 生成），前端按 1–2 → `★☆☆`、3 → `★★☆`、4–5 → `★★★` 展示，
  出题时要求模型对同一知识点的题目给出不同档位。数据完整，视觉与原型一致。
- **B**：直接只生成 1–3。

### Q5 微信登录：AppID / AppSecret 怎么处理？

你没有提供 AppID/AppSecret（`.env` 里当前只有 DeepSeek 的 Key）。另外我查过本机
`C:\Program Files (x86)\Tencent`、`D:\Program Files\Tencent`，**没有安装微信开发者工具**。

- **A（我的建议）**：后端同时实现两条路径 ——
  ① 真实的 `code2session`（`POST /api/auth/login`，用 `WECHAT_APPID`/`WECHAT_SECRET` 换 openid）；
  ② 本地开发开关 `DEV_LOGIN_ENABLED=true` 时，允许用 `code="dev_xxx"` 直接生成稳定的假 openid，
  这样 pytest 与 H5 预览都能跑通，等你填了 AppID 就自动走真实链路（不用改代码）。
  前端照常调用 `Taro.login()` 拿 code。
- **B**：你现在给我 AppID/AppSecret，我只做真实链路（但本地无法自动化测试登录）。
- 无论选哪个：**真机预览需要你安装微信开发者工具并登录**；我这边用「pytest 全绿 + H5 构建 +
  截图与原型逐屏比对」来验证 UI 与交互。

### Q6 LLM 调用：开发自测要不要真实花钱？

`.env` 里已经配了真实的 DeepSeek Key（我只检测了「是否还是占位符」，不会读取或回显你的 Key）。

- **A（我的建议）**：所有自动化测试用 `MockProvider`（**0 成本、不依赖网络**）；
  只在每个阶段末尾做 **1 次真实端到端冒烟**（1 次大纲 + 1 次出题 15 题，约 0.02–0.05 元），
  用来验证「真实 JSON 能过我的质量校验」。约 6 次冒烟 ≈ 0.3 元以内。
- **B**：全程只用 Mock，不做真实调用（我会把真实冒烟留给你自己跑，我给你命令）。

### Q7 单用户每日生成次数上限设多少？

PRD M3-05 要求「单用户每日生成次数限制（防滥用）」，但没给数值。

- **A（我的建议）**：大纲生成 20 次/日、出题生成 20 次/日（足以覆盖自测与内测，且能防脚本狂刷），
  配置项写在 `.env`，随时可调。
- **B**：你给具体数字。

### Q8 代码组织与命名

- **A（我的建议）**：`ai-learn-mini/backend/`（FastAPI）+ `ai-learn-mini/frontend/`（Taro），
  设计文档放仓库根目录；新增 `docs/` 存放接口与测试报告。
- **B**：你指定目录名（例如 `server/` + `miniapp/`）。

### Q9 Git 提交

- **A（我的建议）**：我只写代码不自动提交，每个阶段结束把改动清单给你，你想提交再提交。
  （当前仓库有未提交改动：`prototype/` 被删除、`产品需求分析文档.md` 为新文件，我不会碰它们。）
- **B**：每完成一个阶段我就提交一次（需要你允许我 `git add/commit`）。

### Q10 埋点与合规元素

- **A（我的建议）**：埋点（附录 B 的 5 个事件）**本次不做**（PRD 标注为可选）；
  合规元素按 PRD M8 做：题目/讲解区显示「内容由 AI 生成」、P5 底部「用户协议 · 隐私政策」入口、
  题目举报入口（原型 P3-8 已有）。
- **B**：埋点也要（我在后端加 `event` 表 + 5 个埋点接口，前端在对应时机上报）。

---

## 3. 总体架构

```
┌─────────────────────────── 微信小程序（Taro 4 + React 18 + TS strict） ───────────────────────────┐
│  pages/index   pages/outline   pages/quiz   pages/result   pages/mine                             │
│  components/ (Yanbao / ClayButton / ClayCard / OptionItem / ProgressBar / Chip / TipBar /          │
│               Toast / Dialog / EmptyState / Skeleton / Stepper)                                    │
│  services/ (request.ts 统一 baseURL+token+错误提示+超时重试 | api.ts | audio.ts | storage.ts)       │
│  store/ (UserContext 登录态 | AttemptContext 当前闯关进度)                                          │
│  styles/tokens.scss (色彩/圆角/描边/阴影/字号令牌，与原型一一对应)                                   │
└───────────────────────────────────────┬───────────────────────────────────────────────────────────┘
                                        │ HTTPS + Authorization: Bearer <token>
                                        │ 统一返回 {code, message, data}
┌───────────────────────────────────────▼───────────────────────────────────────────────────────────┐
│                              FastAPI 后端（Python 3.12+ / SQLAlchemy 2.x / Pydantic v2）            │
│  api/        auth · health · knowledge · attempt · question(report) · user(stats)                  │
│  services/   outline_service · question_service · quality(移植 quality_check) ·                    │
│              attempt_service(判分/结算/星级/薄弱点) · mistake_service · quota(限流) · wechat        │
│  services/llm/  LLMProvider(ABC) ├── DeepSeekProvider ├── MockProvider ├── (Bailian/Volcengine 预留)│
│  models/     user · knowledge_source · knowledge_outline · level · question · attempt ·            │
│              answer_record · mistake · question_report（9 张表，含 2.4 节全部索引）                 │
└───────────────────────────────────────┬───────────────────────────────────────────────────────────┘
                                        │ SQLAlchemy ORM
                              SQLite（开发） → PostgreSQL（生产，仅改连接串）
```

**关键架构决策（与 PRD/技术选型一致）**

1. **题目下发不含 `answer` / `explanation`**：`GET/POST` 出题接口返回的题目对象里
   只有 `type / difficulty / stem / options`，答案只在 `POST /api/attempt/{id}/answer` 的响应里出现。
2. **LLM 调用全部在服务端**，且必须经过 `LLMProvider.chat_json()`（含 JSON 修复 + 重试 + 校验）。
3. **业务代码不知道模型是谁**：切换供应商只改 `.env` 的 `LLM_PROVIDER`。
4. **每次 LLM 调用落一条日志**（provider / model / 耗时 / prompt_tokens / completion_tokens / 估算成本 / trace_id），
   满足 M1-02 与 M3-05 的成本监控验收。

---

## 4. 后端设计

### 4.1 目录结构

```
backend/
├── app/
│   ├── main.py                     # FastAPI 实例、lifespan、路由注册、异常处理器、CORS
│   ├── core/
│   │   ├── config.py               # pydantic-settings 读 .env（含 DEV_LOGIN_ENABLED / 限额 / 模型）
│   │   ├── response.py             # ok(data) / AppError → {code, message, data}
│   │   ├── errors.py               # 错误码枚举 + 全局异常处理器（HTTPException / 校验 / 兜底 500）
│   │   ├── security.py             # 自定义 token（JWT, HS256）+ get_current_user
│   │   ├── logging.py              # 结构化日志 + trace_id 中间件 + LLM 调用日志
│   │   └── quota.py                # 单用户每日生成次数限制
│   ├── db/  base.py · session.py · init_db.py
│   ├── models/  (9 张表，字段严格按 PRD 2.4)
│   ├── schemas/ (请求/响应模型，题目下发 schema 与作答响应 schema 分离)
│   ├── api/  deps.py · auth.py · health.py · knowledge.py · attempt.py · question.py · user.py
│   └── services/
│       ├── llm/  base.py（LLMProvider 门面：chat / chat_json / cost_estimate）
│       │        presets.py（deepseek / bailian / volcengine 三家预设）
│       │        langchain_provider.py（init_chat_model + with_structured_output + fallback + 重试）
│       │        mock.py（fixture 驱动，离线开发用） · factory.py（按配置装配）
│       ├── prompts.py              # 大纲提示词 + 出题提示词（复用 quality_check.py 已验证版本）
│       ├── outline_service.py      # 拆知识点 + 修复 + 最多 2 次重试
│       ├── question_service.py     # 3 关 × 5 题、覆盖每个知识点、难度梯度、丢弃补足
│       ├── quality.py              # 移植 quality_check.py 的 15 类检查（致命/警告）
│       ├── attempt_service.py      # 判分、幂等、结算、星级、薄弱点、建议文案
│       ├── mistake_service.py      # 错题池写入 + 间隔重复调度纯函数（数据层先做）
│       └── wechat.py               # code2session（含 DEV 假登录）
├── tests/  conftest.py · unit/ · api/ · fixtures/（含 10 类缺陷的坏样本 JSON）
├── alembic/  env.py · versions/0001_init.py
├── pyproject.toml                  # 依赖 + pytest + ruff 配置
└── README.md                       # 启动 / 迁移 / 测试 / Mock 开关说明
```

### 4.2 接口清单（MVP 落地版）

统一前缀 `/api`，统一返回 `{code, message, data}`；除 `login`、`health` 外都要 `Authorization: Bearer <token>`。

| 编号 | 方法 | 路径 | 说明 | 备注 |
| --- | --- | --- | --- | --- |
| A1 | POST | `/api/auth/login` | `{code}` → `{token, user}` | 真实 code2session / DEV 假登录 |
| A2 | GET | `/api/health` | 健康检查 | M1-01 验收 |
| K1 | POST | `/api/knowledge/outline` | `{raw_text}` → `{outline_id, title, points[]}` | 10–30s，前端骨架屏；含敏感词与字数校验 |
| K2 | PUT | `/api/knowledge/outline/{id}` | `{points[]}` → `{ok}` | 保存编辑后大纲（3–5 个，不足 2 个拒绝） |
| K3 | POST | `/api/knowledge/levels` | `{outline_id}` → `{levels[]}` | 生成 3 关 × 5 题；**不含 answer** |
| K4 | GET | `/api/knowledge/outline/{id}` | → `{outline, levels[]}` | 重新进入/恢复用 |
| Q1 | POST | `/api/attempt/start` | `{outline_id}` → `{attempt_id, total_count, levels[]}` | 多条 ongoing 时，先把旧的标记为 abandoned |
| Q2 | POST | `/api/attempt/{id}/answer` | `{question_id, answer[], elapsed_ms}` → `{is_correct, correct_answer[], explanation, combo, correct_count, answered_count}` | 重复提交同一题 **幂等**（返回原判定，不覆盖记录） |
| Q3 | POST | `/api/attempt/{id}/finish` | → `{accuracy, star, duration_ms, correct_count, total_count, points[], weak_points[], advice}` | 结算 + 建议文案 |
| Q4 | GET | `/api/attempt/{id}` | → 完整结算数据（只读回看） | P5 点进 P4 |
| Q5 | GET | `/api/attempts?page=1&size=20` | → `{list[], total, has_more}` | 倒序、上拉加载 |
| Q6 | GET | `/api/attempt/ongoing` | → `{attempt\|null, level_seq, question_seq, answered_count}` | 首页「继续上次的学习」 |
| Q7 | POST | `/api/questions/{id}/report` | `{reason, detail}` → `{ok, report_count, disabled}` | M3-06：同一题 ≥3 次反馈自动暂停下发 |
| U1 | GET | `/api/user/stats` | → `{study_count, answered_count, avg_accuracy, study_days, streak_days}` | **PRD 接口清单缺此项**，P5 概览需要（我补上） |
| T1 | GET | `/api/templates` | → 6 个模板（含预填文本） | 可选：也可前端常量；我按 PRD M10-08 的思路放后端便于以后配置化 |

**错误码枚举**（字符串，前端按 code 决定文案与是否可重试）：

`INVALID_INPUT`（含字数/敏感词）、`UNAUTHORIZED`、`OUTLINE_NOT_FOUND`、`LLM_TIMEOUT`、
`LLM_UNAVAILABLE`（401/402/403，**不向用户暴露技术细节**，文案统一「服务暂时不可用，请稍后再试」）、
`LLM_BAD_FORMAT`、`GENERATION_FAILED`、`QUOTA_EXCEEDED`、`ATTEMPT_NOT_FOUND`、`ATTEMPT_FINISHED`、
`QUESTION_NOT_FOUND`、`INTERNAL_ERROR`。

### 4.3 LLM 适配层（LangChain 版）

**目标**：满足你「必须使用 LangChain」的要求，同时保证**可测试**、**可一键切换 DeepSeek / 百炼 / 火山**。
做法是把 LangChain 关进一个门面后面，业务代码永远只认自研接口。

```
业务层（outline_service / question_service）
        │  只依赖
        ▼
LLMProvider（自研门面，Protocol）
   ├── chat(messages: list[BaseMessage], **kw) -> str
   ├── chat_json(messages, schema: type[BaseModel], **kw) -> BaseModel
   └── cost_estimate(usage) -> float
        ▲
        │  实现（唯一允许出现 langchain 的地方）
┌───────┴──────────────────────────────────────────────────────────────┐
│ LangChainProvider                                                    │
│   model = init_chat_model(model, model_provider="openai",            │
│                           base_url=..., api_key=...,                 │
│                           temperature=..., timeout=...,              │
│                           max_retries=...)                           │
│   structured = model.with_structured_output(schema,                  │
│                           method=settings.llm_structured_method,      │
│                           include_raw=True)                          │
│   + LangChain fallback：主供应商失败 → 备用供应商（可配置）            │
│   + 失败降级：结构化输出失败 → 原始文本 → 三级 JSON 修复              │
└──────────────────────────────────────────────────────────────────────┘
   ├── MockProvider（读 tests/fixtures/llm/*.json，离线/CI 用）
   └── FakeChatModelProvider（测试用，包装 LangChain 官方 GenericFakeChatModel）
```

**三家供应商预设**（全部走 OpenAI 兼容协议，切换只改 `.env`，不动代码）：

| provider | base_url | 默认模型 | 结构化输出方法 | 备注 |
| --- | --- | --- | --- | --- |
| `deepseek` | `https://api.deepseek.com/v1` | 沿用 `.env` 的 `LLM_MODEL`（默认 `deepseek-chat`） | `json_mode` | 开发期默认，价格最低 |
| `bailian` | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `qwen-plus` | `function_calling`（回退 `json_mode`） | 产品化首选，易拿到《合作协议》 |
| `volcengine` | `https://ark.cn-beijing.volces.com/api/v3` | 火山方舟 endpoint id | `function_calling`（回退 `json_mode`） | 备选/灾备 |

对应的 `.env` 变量（新增）：`LLM_PROVIDER`、`LLM_STRUCTURED_METHOD`、`LLM_FALLBACKS`、
`BAILIAN_API_KEY`、`BAILIAN_MODEL`、`VOLCENGINE_API_KEY`、`VOLCENGINE_MODEL`、`VOLCENGINE_BASE_URL`。

**几个实现要点**

1. **结构化输出仍要兜底**：`with_structured_output` 只保证「结构合法」，不保证「教研质量」。
   所以它负责第一层解析，失败时降级到「原始文本 + 三级 JSON 修复（去代码块标记 → 去尾逗号 → 截断补齐）」，
   复用 `quality_check.py` 已验证的逻辑；质量校验（15 项）始终在业务层执行。
2. **错误映射**：HTTP 401/402/403 → 立刻中止、**不重试**，映射为 `LLM_UNAVAILABLE`，
   用户侧统一提示「服务暂时不可用，请稍后再试」；超时/5xx/429 → 指数退避重试（最多 2 次）。
3. **可观测**：每次调用记录 provider / model / 耗时 / token 用量 / 估算成本 / trace_id（M1-02、M3-05 验收）。
4. **依赖收敛**：`langchain` + `langchain-core` + `langchain-openai`（三家都走 OpenAI 兼容协议，
   所以不需要为每家装一个集成包）。如果实测 DeepSeek 的 `json_mode` 在 `langchain-deepseek` 下更稳，
   我再单独加这个包，但接口不变。
5. **DeepSeek 的 JSON 模式有一个硬约束（已实测踩到）**：使用 `response_format=json_object` 时，
   提示词里必须出现 "json" 这个词，否则接口直接返回 400
   （`Prompt must contain the word 'json' in some form`）。适配层在 `json_mode` 下会统一追加一条
   「只输出 json 对象 + 字段结构说明」的 system 提示，**业务提示词不需要各自记得这件事**。
   > 实测记录（2026-09-17）：`deepseek-flash` + `json_mode` 一次结构化调用成功，
   > 耗时 6.3s、输入 177 token / 输出 1117 token、约 0.0046 元。
6. **fallback 放在门面层而不是 LangChain 的 `with_fallbacks()`**：因为
   `chat_model.with_fallbacks([...])` 返回的是 Runnable，会丢掉 `with_structured_output`
   （而结构化输出正是我们解析题目的主路径）。门面级 fallback 对 Mock/Fake 同样生效，也更好单测。

**测试策略（可测试是本设计的第一约束）**

| 层次 | 手段 | 覆盖 |
| --- | --- | --- |
| 单元测试 | LangChain 官方 `GenericFakeChatModel(messages=iter([...]))` 注入 `LangChainProvider` | 结构化输出解析、重试次数、401/402/403 不重试、超时重试、错误码映射 |
| 单元测试 | 自研 `MockProvider`（fixture JSON） | 大纲/出题服务逻辑与质量校验（含 10 类缺陷样本） |
| 接口 e2e | `MockProvider` + httpx ASGITransport | 登录 → 大纲 → 出题 → 15 题作答 → 结算 全链路 |
| 真实冒烟 | 阶段末 1 次真实调用（Q6 选 A） | 验证真实 JSON 能过质量校验；连续 3 次无解析失败 |

### 4.4 出题引擎与质量校验（MVP 的技术护城河）

1. **提示词直接复用** `quality_check.py` 的 `SYSTEM_PROMPT` + `USER_TEMPLATE`（已验证 20/20、99.2 分），
   只做两处扩展：题目数改为 15（3 关 × 5 题）、新增「每个知识点至少被 1 题覆盖」与「难度要有梯度」的硬约束。
2. **JSON 解析三层兜底**（复用已验证逻辑）：直接 `json.loads` → 去尾逗号/去代码块标记 → 截断补齐；
   仍失败则重新调用（最多 2 次），再失败返回 `LLM_BAD_FORMAT`。
3. **服务端质量校验**：把 `quality_check.py` 的检查项移植为 `services/quality.py`，
   **致命级**（答案不在选项 / 选项 key 跳号 / 选项重复 / 判断题选项数≠2 / 单选题选项数≠4 /
   多选正确答案 <2 / 「以上都对」类选项 / 题干重复且选项重合 / 知识点引用不存在）
   → 单题重新生成（最多 2 次）→ 仍不合格则丢弃该题，从备用题补足，保证每关 5 题；
   **警告级**（讲解过短 / 答案集中 / 难度全同 / 知识点未覆盖 / 选项组重复用于 ≥3 题）→ 只记日志。
   刻意**不检查**的三类（选项高度相似、句式相同的并列出题、讲解里解释错误选项）保持不检查，
   避免把好题误判成缺陷。
4. **成本控制**：单次完整学习（15 题）目标 ≤0.05 元；每次调用记录 token 与耗时。

### 4.5 判分与结算规则（实现细节，先对齐口径）

| 项 | 规则 |
| --- | --- |
| 单选 / 判断 | 选中集合与答案集合完全相等才算对 |
| 多选 | **全对才算对，不设部分得分**（少选、多选都算错） |
| 用时 | 每题 `elapsed_ms` 由前端计时上传；总用时 = 该次 attempt 的首题开始到 finish 的时间（服务端时间戳为准） |
| 正确率 | `correct_count / total_count × 100`，保留 1 位小数 |
| 星级 | ≥90% 三星 / 70–89% 两星 / 50–69% 一星 / **<50% 零星**（Q3-1 若改为原型口径我再调） |
| 薄弱知识点 | 该知识点正确率 <60%，最多列 3 个 |
| 建议文案 | 命中薄弱点时：「你在「X」上正确率 40%，建议重点复习这一块」；无薄弱点时：肯定 + 下一步建议 |
| 错题池 | 答错即写入 `mistake`（`wrong_count +1`、`mastery` 下调、`next_review_at = now + 1 天`），MVP 只落库不出界面 |
| 幂等 | 同一 `attempt` 内同一 `question_id` 重复提交返回首次判定，不重复计数 |

---

## 5. 前端设计（Taro 4 + React 18 + TS）

### 5.1 工程与配置

- `taro init` 选 **React + TypeScript**，`pnpm` 管理，开 `strict: true`。
- `config/index.ts`：`framework: 'react'`、`designWidth: 375`、`deviceRatio: {375: 2 / 1}`。
  > 说明：原型是按 375 逻辑像素的 iPhone 稿画的（字号 14.5px、卡片 24px、描边 2.5px），
  > 所以 `designWidth` 取 **375**，这样原型里的 px 数值可以**原样搬进样式**，是 1:1 还原的前提。
- `app.config.ts`：`pages` 5 个页面 + `tabBar`（首页 / 我的，选中色 `#33302E`、背景 `#FFFFFF`、边框 `#33302E`）。
- `styles/tokens.scss`：把第 1.3 节的令牌写成 SCSS 变量 + Taro 全局样式，页面只引用变量，不写死色值。

### 5.2 公共组件（对应原型第 7、8 节与「状态与规范」页）

| 组件 | 覆盖状态 |
| --- | --- |
| `Yanbao` | 8 个表情（原样移植原型 SVG） |
| `ClayButton` | 主 / 次 / 危险 / 文字 / 禁用；高度 52 与 44；按压下沉 2px + 投影收 0 |
| `ClayCard` | 一级（硬投影）/ 二级（硬投影 + 柔光 + 内高光） |
| `OptionItem` | 默认 / 已选（暖黄 + 下沉）/ 答对（薄荷绿 + 「答对了」角标）/ 答错（珊瑚红 + 晃动）/ 正确答案高亮（薄荷绿 + 角标）/ 其余降透明虚线 / 禁用；判断题大块变体 |
| `ProgressBar` | 12px 高、2px 墨黑描边、暖橙填充；关卡完成时的「唰」填充动画 |
| `Chip` / `TipBar` / `Toast` | 5 种颜色标签；暖黄提示条；珊瑚红错误提示 |
| `Skeleton` | shimmer 骨架屏（`@keyframes shim` 与原型一致） |
| `Dialog` | 圆角 24 + 硬投影 + 底部按钮；退出确认 / 举报两个场景 |
| `EmptyState` | 空数据 / 网络错误 / 生成失败 三个变体 |
| `Stepper` | 「① 理解内容 ② 梳理结构 ③ 准备出题」循环高亮 |

图标：按原型第 8 节的 12 个内联 SVG（线性、2px 描边、圆头端点）移植为 `Icon` 组件，
**不使用 emoji 作为功能图标**（合规要求）。

### 5.3 页面状态机（严格按原型与 PRD 5.3）

```
答题页：未作答 → 已选待提交 ⇄ 未作答 → 已提交 → 已判定 → 已展示讲解
         → 下一题（本关还有题）/ 本关完成（进入下一关）/ 通关结算（三关全完成）
大纲页：生成中（骨架屏 + 三步循环文案）→ 可编辑 ⇄ 编辑中 → 开始闯关
         ↘ 生成失败（安抚 + 改内容 / 再试一次）
首页：  空态 ⇄ 有内容 ⇄ 校验错误 → 生成；有未完成闯关时显示「继续上次的学习」
```

细节实现点：

1. **提交前可改答案、提交后锁定**；多选可反复勾选与取消。
2. **答对答错都展示讲解**（核心价值，不做开关）。
3. **反馈动效**：答对选项弹入对勾、答错轻晃 2 次（≤300ms）、连对 3 题弹跳放大 + 速度线横幅。
4. **音效**：`Taro.createInnerAudioContext()`，首次用户交互（点「开始生成」）时预加载；
   处理静音键与 `prefers-reduced-motion`（动效降级为淡入淡出）。
5. **震动**：答对 `Taro.vibrateShort({type:'light'})`、连对 `medium`、答错不震动。
6. **进度保存**：每次提交后写 `Taro.setStorageSync`（本地秒开）+ 服务端 `answer_record`；
   切后台/退出不丢；网络失败时本地暂存并在恢复后重试（`request.ts` 内置 2 次重试）。
7. **长任务体验**：K1/K3 等待 10–60s，骨架屏 + 循环文案 + 超时（30s 大纲 / 60s 出题）后给重试按钮。
8. **只读模式**：从 P5 记录进入 P4 时 `readonly`，不可重新作答、不显示「下一题」。

### 5.4 前端常量

- 6 个模板（Q3-4）：行测·资料分析 / 公基·法律常识 / 考研政治 / 英语语法 / 编程概念 / 自定义，
  预填文本按 PRD F1 表格；模板文案与预填内容我会给你一份清单确认后再定稿（避免我凭空写内容）。
- 循环加载文案：「正在理解你的内容…」→「正在梳理知识结构…」→「快要好了…」（PRD F2）。

---

## 6. TDD 自测方案（后端强制）

### 6.1 工具与约定

- `pytest` + `pytest-asyncio` + `pytest-cov` + `httpx`（`ASGITransport` 直连 FastAPI，不起端口）
- 测试库：每个测试用 `tmp_path` 下独立 SQLite 文件（不是内存库，保证事务与迁移脚本行为一致）
- LLM：`MockProvider`（fixture 驱动），**测试永不联网、永不花费**
- 顺序：**先写测试（红）→ 实现（绿）→ 重构**；每个服务的 bug 修复都要先补一条回归测试

### 6.2 测试矩阵（约 90+ 用例，按模块）

| 模块 | 关键用例（节选） |
| --- | --- |
| `quality.py` | 15 类检查项逐项命中；**3 类刻意不报的反例必须不报**（最小对照选项、句式并列出题、讲解里解释错误选项）；与 `quality_check.py` 的 **parity 测试**（同一份坏样本，问题码集合一致） |
| `llm/deepseek.py` | 正常返回；代码块包裹；尾逗号；截断 JSON；非 JSON；HTTP 401/402/403 立即中止不重试；超时重试 2 次后抛 `LLMTimeout`；token 与耗时被记录 |
| `outline_service.py` | 3–5 个知识点；返回 2 个或 6 个 → 重试/纠正；结构非法 → 修复 → 重试 2 次 → `LLM_BAD_FORMAT`；超时；敏感词/字数校验 |
| `question_service.py` | 15 题 / 3 关 / 每关 5 题；每个知识点至少 1 题；难度梯度；致命缺陷题被替换；替换失败则丢弃并从备用题补足到 5 题；整份失败不落脏数据（事务回滚） |
| `attempt_service.py` | 单选题/判断题判分；多选少选/多选多选都判错、全对判对；重复提交幂等；答错写 `mistake`；题目下发**不含 answer**（序列化断言）；结算正确率/星级/薄弱点/建议文案；全部答错、全部答对的边界；已 finish 的 attempt 再作答 → `ATTEMPT_FINISHED` |
| `auth.py` | 登录建号/复用；token 签发与过期；伪造 token 拒绝；写接口未登录 401；**不接受前端传 user_id** |
| `quota.py` | 第 21 次生成被拒；次日重置；计数按用户隔离 |
| `question_report` | 落库；同一题 3 次 → 标记待复核；被标记题目不再下发 |
| `api/` e2e | 完整链路：登录 → 大纲 → 编辑保存 → 出题 → start → 15 次作答 → finish → 查详情 → 记录列表 → ongoing 恢复 → 举报 |
| 迁移 | `alembic upgrade head` 后表结构与索引齐全（断言索引存在） |

### 6.3 验收口径

1. `pytest` 全绿；**核心服务层与 API 层覆盖率 ≥85%**（`pytest --cov=app --cov-report=term-missing`）。
2. `python quality_check.py --reaudit reports/raw_*.json` 仍可跑（我保留脚本原样，作为回归基线）。
3. 阶段 6 做 1 次真实 LLM 冒烟（Q6 选 A 时），连续 3 次无 JSON 解析失败、无致命质量问题。
4. 前端：`pnpm build:weapp` 与 `pnpm build:h5` 零错误；H5 构建后逐屏截图与原型 PNG 比对（颜色/圆角/描边/文案）。

---

## 7. 需要安装的依赖（要联网，会单独向你申请授权）

**后端**（用本机已有的 `uv` 建 `.venv`）：`fastapi`、`uvicorn[standard]`、`sqlalchemy`、`alembic`、
`pydantic`、`pydantic-settings`、`httpx`、`python-dotenv`、`pyjwt`、
`langchain`、`langchain-core`、`langchain-openai`、
`pytest`、`pytest-asyncio`、`pytest-cov`、`ruff`。

**前端**：`@tarojs/cli` 等 Taro 4 全家桶、`react`、`react-dom`、`typescript`、`@nutui/nutui-taro`、`sass`。

> `uv` 默认会下载一个独立的 Python（我建议 3.12，**原因：本机只有 Python 3.14.5，
> 而 pydantic-core / SQLAlchemy 的 3.14 预编译轮子存在不确定性**；用 3.12 能避免"装不上依赖"这类非业务问题，
> 项目代码仍然是 3.11+ 兼容写法）。
> 如果你希望就用系统 3.14，我会先做一次安装验证，装不上再回退到 3.12。

---

## 8. 执行步骤（确认后按此顺序推进，每阶段结束给你验收物）

| 阶段 | 内容 | 产出 / 验收 |
| --- | --- | --- |
| **M0 环境与骨架（0.5 天）** | 装依赖（uv 建 venv / Taro init）；后端 `main.py` + `config.py` + 统一响应 + 全局异常 + trace_id 日志；`GET /api/health`；前端 Taro 工程可 `build:weapp` | `pytest` 绿；`uvicorn app.main:app --reload` 起得来；Swagger 可打开；Taro 空工程编译通过 |
| **M1 数据层（0.5 天）** | 9 张表模型 + 索引 + Alembic 首个迁移 + CRUD 封装（**先写测试**） | `alembic upgrade head` 通过；模型与索引断言测试通过 |
| **M2 LLM 适配层 + 出题引擎（1.5–2 天）** | LangChain 版 `LLMProvider`（`init_chat_model` + `with_structured_output` + fallback）+ 三家预设 + Mock/Fake 双轨；提示词移植；JSON 修复与重试；`quality.py` 移植 + parity 测试；大纲生成；15 题生成与补足 | 全部用例绿（零网络）；构造含 10 类缺陷的输入能被拦截并替换；改 `.env` 能切到百炼/火山 |
| **M3 业务接口（1.5 天）** | 登录/鉴权、大纲保存、关卡下发、start/answer/finish、记录列表、ongoing、举报、统计、限流（全部 TDD） | Swagger 手动走通「登录→生成→答题→结算」；e2e 用例绿；覆盖率 ≥85% |
| **M4 前端骨架（1 天）** | 设计令牌、12 个图标、10 个公共组件、`request.ts`（token 注入 / 错误提示 / 超时重试）、Context（用户态 + 闯关态）、静默登录、tabBar | 组件在 H5 里逐状态截图与「状态与规范」页一致 |
| **M5 五个页面（2.5 天）** | P1–P5 共 18 屏状态 1:1 还原 + 动效/音效/震动/进度保存/只读模式/网络异常 | H5 截图逐屏比对原型；真机清单（需你装开发者工具） |
| **M6 联调与回归（0.5 天）** | 联调异常路径（超时/断网/重复提交/中途退出）、边界（20 字 / 2000 字 / 只留 1 个知识点 / 全错）、真实 LLM 冒烟、`quality_check.py --reaudit` 回归 | 测试报告 + 截图对照表 + README（启动/测试/联调说明） |

总计约 8 个工作日的工作量，按阶段交付，你可以在每个阶段验收后再进入下一阶段。

---

## 9. 风险与对策

| # | 风险 | 对策 |
| --- | --- | --- |
| 1 | Python 3.14 装不上 `pydantic-core` / SQLAlchemy 轮子 | 用 `uv` 装独立 3.12；先做安装验证再写代码 |
| 2 | 本机无微信开发者工具、无 AppID | 提供 `DEV_LOGIN_ENABLED` 假登录；用 H5 构建验证 UI；真机预览需要你补工具与 AppID |
| 3 | 网络受限（装依赖、真实 LLM 调用） | 每次需要联网我单独申请授权，不擅自行动 |
| 4 | 出题 15 题耗时较长（10–60s），可能超时 | 后端超时 + 重试 2 次；前端骨架屏 + 循环文案 + 明确的重试出口；失败不留脏数据 |
| 5 | 真实模型偶发 JSON 损坏 | 三层修复 + 重试 + 单题补足；真实冒烟连续 3 次作为验收 |
| 6 | Taro 4 + pnpm 在 Windows 上的构建坑 | 先用最小工程跑通 `build:weapp`/`build:h5`，再开始写页面；锁定版本号 |
| 7 | 我在 UI 细节上「自行发挥」 | 已把 4 处冲突（Q3）与 2 处原型缺口（Q1 上传入口、Q3-4 后 3 个模板）显式列出来问你，未确认前不写这几处 UI |
| 8 | LangChain 版本迭代快（1.x 起 API 与旧版差异大，网上大量示例是过时写法） | 锁定 `langchain` / `langchain-core` / `langchain-openai` 的**具体版本号**并写进 `pyproject.toml`；只用 Context7 拉到的当前官方 API（`init_chat_model` / `with_structured_output` / `GenericFakeChatModel`）；框架被限制在 `services/llm/` 一层，出问题可替换而不动业务 |

---

## 10. 明确不做（MVP 边界，与你文档一致）

视频/音频/网页解析、支付与会员、好友 PK 与排行榜、分享海报、题库体系、后台管理系统、
多端发布（H5 只用于本地验证 UI）、深色模式、关卡地图、自适应难度、苏格拉底引导、
间隔重复的可视化界面（数据层照做）、复盘报告图表、埋点（除非 Q10 选 B）。

---

## 11. 附：我在写这份方案前已经核实过的事实

| 事实 | 结论 |
| --- | --- |
| 本机环境 | Python 3.14.5（`D:\Program Files\Python`）、Node 24.16.0、pnpm 11.19.0、uv 0.11.19、git 2.54.0 |
| 依赖现状 | `fastapi / sqlalchemy / pydantic` **均未安装**，需联网安装 |
| DeepSeek Key | `.env` 里已是真实 Key（不是占位符），我不会读取或回显它的内容 |
| 微信开发者工具 | 未安装（已检查两处常见安装路径） |
| 仓库状态 | `ai-learn-mini` 是独立 Git 仓库；有未提交改动（`prototype/` 删除、`产品需求分析文档.md` 新增），我不动它们 |
| 技术文档核对 | Taro 4 的 `taro init` + `app.config.ts`（pages/tabBar）+ `config/index.ts`（`defineConfig`、`designWidth`、`framework: 'react'`）；FastAPI 的 `lifespan`、`@app.exception_handler`、全局 `dependencies`、Pydantic Settings；SQLAlchemy 2.0 的 `DeclarativeBase` + `Mapped` + `mapped_column` + `JSON` 类型与 `type_annotation_map` —— 以上均通过 Context7 拉取了**当前官方文档**核对，实现时会按模块再次核对细节（如 Alembic 迁移、PyJWT、NutUI 组件用法） |

> 说明：你提到的 LangChain，技术选型文档第 10 节明确「不建议引入」，所以我按
> `httpx` + 自研适配层实现（第 1.2 节）。这一条如果你要改，请直接说。

---

---

## 附录 A：变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-09-17 | 首版（开发前确认稿） |
| v1.1 | 2026-09-17 | **第 2 章 10 项决策全部确认（Q1A–Q10A）**；按你的要求把 LLM 层由「httpx 直连 + 自研适配层」改为**必须使用 LangChain**（新增 4.3 节、依赖清单、风险 8、M2 调整），并保留自研门面以满足「可测试、可切换百炼/火山」两条硬要求 |
| v1.2 | 2026-09-17 | M0/M1/M2 实施记录：补齐 4.3 节的实现细节（DeepSeek `json_mode` 必须出现 "json" 的硬约束、fallback 放在门面层的原因），并记录首次真实模型冒烟结果 |
| v1.3 | 2026-09-17 | 约定：**每个阶段完成后输出结构化的《MVP 阶段成果总结》**（后端模块表 → 前端模块表 → 测试与验证 → 未完成与下一步），成果随时间累积记录在仓库根目录的 `MVP阶段成果总结.md` |
| v1.4 | 2026-09-17 | M2 完成：提示词、质量校验（与 `quality_check.py` 逐项对齐并加 parity 测试）、大纲生成、出题引擎（单题重生/丢弃/补题）全部落地；真实端到端冒烟通过（15 题、3 关各 5 题、0 致命 0 警告），实测成本 0.0677 元/次，略超 PRD 的 ≤0.05 元目标，优化方案留 M6 决策 |
| v1.5 | 2026-09-17 | M3 完成：登录鉴权（JWT + 微信 code2session + DEV 假登录）、知识接口 K1–K4、答题接口 Q1–Q6、举报下线（含 `question.disabled_at` 迁移 0002 与人工处理接口）、我的页统计、每日限流全部落地；后端闭环可用，测试 216 个全绿、覆盖率 94%。记录两点决策：① 题目下发结构独立定义，答案只在提交后返回；② 「3 人举报下架」按**去重人数**计算（MVP 内每套题不共享，实际下架入口是人工接口） |

### 已确认的决策（2026-09-17）

| 编号 | 决策项 | 结果 |
| --- | --- | --- |
| Q1 | 输入形式 | **A**：MVP 只做文本框输入（20–2000 字），文档解析留 V1.0 |
| Q2 | 扩展 7 屏 | **A**：本次只做 P1–P5（18 屏状态全覆盖） |
| Q3 | UI 与 PRD 的 4 处冲突 | **全部同意我的默认**：星级逻辑按 PRD（<50% 零星）+ 视觉沿用原型；「先看看错题」MVP 不跳死链；P5 概览 3 格、记录项按原型配色；首页模板按 PRD 做 6 个横向滑动卡 |
| Q4 | 难度展示 | **A**：库里存 1–5，前端 1–2→★☆☆、3→★★☆、4–5→★★★ |
| Q5 | 微信登录 | **A**：真实 `code2session` + `DEV_LOGIN_ENABLED` 假登录双路径 |
| Q6 | LLM 自测花费 | **A**：自动化测试全用 Mock/Fake（0 成本），每阶段末 1 次真实冒烟（合计约 0.3 元内） |
| Q7 | 每日限流 | **A**：大纲 20 次/日、出题 20 次/日（`.env` 可调） |
| Q8 | 目录命名 | **A**：`backend/` + `frontend/` |
| Q9 | Git | **A**：不自动提交，每阶段给改动清单 |
| Q10 | 埋点与合规 | **A**：埋点不做，合规元素（AI 生成标注 / 协议入口 / 举报入口）按原型做 |
| 额外 | AI 框架 | **必须使用 LangChain**，同时满足「可测试」「可切换百炼 / 火山」 |

*下一步：进入 M0（环境与骨架）。*
