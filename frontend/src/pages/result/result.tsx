import { View } from '@tarojs/components'

import './result.scss'

/** P4 通关结算 —— M5 阶段按原型 P4-1 三星 / P4-2 低分鼓励 两态实现（含只读回看模式） */
export default function Result() {
  return <View className='page-result' />
}
